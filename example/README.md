# Example project

This module contains a deliberately broken Java package used to demonstrate the import analyzer.

## Running the demo

```
./gradlew :example:run
```

You can also run the CLI against this module from the repository root:

```
./gradlew :cli:run --args="analyze --project example"
```
